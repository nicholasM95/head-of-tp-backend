package be.nicholasmeyers.headoftp.device.repository;

import be.nicholasmeyers.headoftp.device.domain.DeviceLocation;

public interface DeviceLocationRepository {

    void createDeviceLocation(DeviceLocation deviceLocation);
}
